name: sway
description: >-
  Type-safe Flutter localization with an instant in-app locale switcher and
  zero-friction migration from ARB, easy_localization, and slang.
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
