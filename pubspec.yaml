name: basepackage
description: >-
  Private Darkmintis Flutter package template — package, example, tests, and
  pub.dev-ready CI. Clone and rename to start a new open-source package.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
